/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.livemint.com",
      "oaidalleapiprodscus.blob.core.windows.net",
      "www.databricks.com",
      "petapixel.com",
    ],
  },
};

module.exports = nextConfig;
